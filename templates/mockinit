{{ header }}
package mock

import "{{ modpath }}/{{ .Package }}"

type mock struct {
    memory map[string][]interface{}
}

func New() {{ package .Package }}.DB {
    return &mock{memory: make(map[string][]interface{})}
}