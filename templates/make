{{ header }}
package {{ .Package }}

{{ if .Imports }}
import (
    {{ range .Imports }}
    "{{ . }}"
    {{ end }}
)
{{ end }}

type {{ .Model }} struct {
	{{- range $col := .Columns }}
	{{ .Name }} {{ .Type }} `db:"{{ .DBName }}"{{ range .Tags }} {{ . }}:"{{ camel $col.Name }}{{ if $col.Nullable }},omitempty{{ end }}"{{ end }}`
	{{- end }}

	db *DB
}